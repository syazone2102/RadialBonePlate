{
  "name": "radialboneplate-cdss",
  "version": "1.0.0",
  "description": "Radial Bone Plate Clinical Decision Support System",
  "main": "index.html",
  "scripts": {
    "build:android": "npx cap sync android && npx cap open android",
    "sync": "npx cap sync",
    "add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
